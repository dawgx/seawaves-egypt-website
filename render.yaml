services:
  # Full-Stack Service (React App + API)
  - type: web
    name: sea-waves-website
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: EMAIL_USER
        value: settlethailand@gmail.com
      - key: EMAIL_PASS
        value: xjeincwfnyvlcezi
      - key: EMAIL_SERVICE
        value: "gmail"
